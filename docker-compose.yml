services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: plu_assistant
      POSTGRES_USER: plu
      POSTGRES_PASSWORD: plu
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
